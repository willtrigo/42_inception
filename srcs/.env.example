# Inception Environment Template — Copy to .env and customize
# Required: Replace placeholders (e.g., DOMAIN_NAME) before running `make up`.

# Domain (mandatory: your 42 login + .42.fr)
DOMAIN_NAME=yourlogin.42.fr  # e.g., dande-je.42.fr

# MariaDB Configuration
MYSQL_DATABASE=wordpress
MYSQL_USER=wpuser
MYSQL_ROOT_PASSWORD_FILE=$HOME/42_inception/secrets/db_root_password.txt
MYSQL_PASSWORD_FILE=$HOME/42_inception/secrets/db_password.txt
MYSQL_PORT=3306

# WordPress Configuration
WORDPRESS_DB_HOST=mariadb  # Internal service name
WORDPRESS_DB_USER=wpuser  # Matches MYSQL_USER
WORDPRESS_DB_PASSWORD_FILE=$HOME/42_inception/secrets/db_password.txt
WORDPRESS_DB_NAME=wordpress  # Matches MYSQL_DATABASE
WORDPRESS_TABLE_PREFIX=wp_  # Default; customize for multi-site
WORDPRESS_CONFIG_EXTRA=(string)define('WP_DEBUG', true);  # Extra wp-config.php defines (PHP syntax)
WORDPRESS_ADMIN_USER=wpsuper  # Admin username (avoid 'admin')
WORDPRESS_ADMIN_PASSWORD_FILE=$HOME/42_inception/secrets/wp_admin_password.txt
WORDPRESS_ADMIN_EMAIL=admin@yourlogin.42.fr  # Replace with your email

# NGINX TLS (self-signed defaults; override for prod CA certs)
NGINX_TLS_CN=yourlogin.42.fr  # Matches DOMAIN_NAME
NGINX_TLS_DAYS=365  # Cert validity in days

# Project-Wide
COMPOSE_PROJECT_NAME=inception  # Docker project prefix
DOCKER_BUILDKIT=1  # Enable BuildKit (faster, layered builds)
